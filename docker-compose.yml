version: "3.8"

services:
  sdkpt-ml:
    container_name: sdkpt-ml
    build:
      context: .
      dockerfile: dev.dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
